# Image processing
numpy==1.24.3
opencv-python==4.7.0.72

# Device connection
adbutils==0.11.0
uiautomator2==2.16.17
uiautomator2cache==0.3.0.1
wrapt==1.15.0
psutil==6.1.1
pywin32==306

# OCR
# PP-OCRv6 引擎。onnxruntime 发行版故意不在这里声明：
# onnxruntime 与 onnxruntime-directml 装的是同一个模块但是两个发行版名，
# 一旦写进 requirements，pip 每次更新都会把 GPU 版覆盖成 CPU 版。
# 版本与安装时机统一由 deploy/ocr_deps.py 管理。
rapidocr==3.9.2
cn2an==0.5.23

# Web
paho-mqtt==1.6.1
fastapi==0.104.1
python-multipart==0.0.9
uvicorn==0.38.0
websockets==13.1
zerorpc==0.6.3
pydantic==2.10.0

# Utils
rich==13.3.5
pyyaml==6.0
tqdm==4.65.0
anytree==2.8.0
cryptography>=42.0.7
onepush==1.3.0
inflection==0.5.1
schedule==1.2.2
nest_asyncio==1.6.0

# Data（points.py 用 scipy；DemonEncounter 数据导出用 pandas）
scipy==1.15.3
pandas==2.3.3

# Frida (for AutoCheckinBigGod)
frida==17.6.2
frida-tools==14.5.2

oashya
oas-checkin-biggod

# For dev
pyperclip==1.11.0